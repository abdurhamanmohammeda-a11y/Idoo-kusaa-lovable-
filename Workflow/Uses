name: Dependent Workflow

on:
  workflow_call:
    inputs:
      message:
        required: true
        type: string

jobs:
  print_message:
    runs-on: ubuntu-latest
    steps:
      - name: Print message
        run: echo "Message from caller: ${{ inputs.message }}"
